window.STRIDEWORDS_COMMUNITY = {
  // Set a Cloudflare Workers / Firebase / Supabase endpoint here to share posts across devices.
  // When empty, the community pages run as a browser-local preview using localStorage.
  endpoint: "",
  storagePrefix: "stridewords-community-v1"
};
